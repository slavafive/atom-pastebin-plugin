'use babel';

import AtomPastebinPluginView from '../lib/atom-pastebin-plugin-view';

describe('AtomPastebinPluginView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
